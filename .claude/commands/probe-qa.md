Use the @probe-qa agent for interactive QA testing of the following: $ARGUMENTS

If no arguments were provided, ask the user what service/feature needs interactive testing. Probe spins up tmux sessions, sends real commands, captures output, asserts pass/fail, and always cleans up — even on failure.
