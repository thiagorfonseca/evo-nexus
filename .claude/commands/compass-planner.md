Use the @compass-planner agent to help the user create a work plan for the following: $ARGUMENTS

If no arguments were provided, ask the user what they want to plan (feature to build, refactor, bug fix, migration). Compass runs a structured interview and saves the plan to `workspace/projects/plans/` before any handoff to implementation.
