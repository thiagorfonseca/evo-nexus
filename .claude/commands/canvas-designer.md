Use the @canvas-designer agent for UI/UX design and implementation: $ARGUMENTS

If no arguments were provided, ask the user what UI feature is needed. Canvas detects framework first, commits to an aesthetic direction BEFORE coding, picks distinctive typography (no generic fonts), and avoids AI-slop generic patterns.
