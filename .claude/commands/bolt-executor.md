Use the @bolt-executor agent to implement the following: $ARGUMENTS

If no arguments were provided, ask the user what they want implemented (feature, fix, refactor). Bolt prefers the smallest viable change, runs verification after each step, and escalates to @apex-architect after 3 failed attempts on the same issue.
