Use the @vault-security agent to audit the following for security vulnerabilities: $ARGUMENTS

If no arguments were provided, ask the user what to audit (specific module, recent commit, full codebase). Vault runs OWASP Top 10, secrets scan, dependency audit. READ-ONLY. Outputs severity-rated findings prioritized by severity × exploitability × blast radius, with secure code examples.
