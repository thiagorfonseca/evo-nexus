Use the @oath-verifier agent to verify completion of the following work: $ARGUMENTS

If no arguments were provided, ask the user what should be verified (specific feature, recent commit, acceptance criteria from a plan). Oath runs verification commands itself (tests, type checks, builds) and demands fresh evidence — never trusts "should work" claims.
