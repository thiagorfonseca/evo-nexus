Use the @hawk-debugger agent to investigate and fix the following bug or build error: $ARGUMENTS

If no arguments were provided, ask the user to describe the bug, error message, or failing build. Hawk reproduces before investigating, finds root cause (not symptoms), tries one hypothesis at a time, and escalates to @apex-architect after 3 failed attempts.
