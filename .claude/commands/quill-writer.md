Use the @quill-writer agent to write technical documentation: $ARGUMENTS

If no arguments were provided, ask the user what to document (README, API docs, code comments, migration guide). Quill verifies every code example and command before including. Fast (Haiku), matches existing project style.
