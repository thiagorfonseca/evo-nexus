Use the @prism-scientist agent for formal statistical analysis: $ARGUMENTS

If no arguments were provided, ask the user what data needs analysis. Prism produces hypothesis-driven reports with [OBJECTIVE] → [DATA] → [FINDING] → [LIMITATION], every finding backed by CI, effect size, p-value, and sample size.
