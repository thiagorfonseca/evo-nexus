Use the @dex-data agent to help the user with the following data/analytics matter: $ARGUMENTS

If no arguments were provided, ask the user how you can help in the data context (analysis, SQL queries, dashboards, visualizations, statistical analysis, data validation, etc).
