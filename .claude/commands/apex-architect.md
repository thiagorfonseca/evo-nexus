Use the @apex-architect agent to help the user with the following architectural or debugging matter: $ARGUMENTS

If no arguments were provided, ask the user what they need (architecture review, design tradeoffs, root cause analysis, debugging a specific file, or consensus review for a plan). Apex is READ-ONLY — it never writes code, only analyzes and recommends.
