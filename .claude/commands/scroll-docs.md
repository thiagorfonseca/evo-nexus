Use the @scroll-docs agent to look up external documentation: $ARGUMENTS

If no arguments were provided, ask the user what SDK/API/framework reference is needed. Scroll prefers local repo docs first, then official sources, with citations and version compatibility notes. READ-ONLY.
