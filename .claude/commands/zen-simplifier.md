Use the @zen-simplifier agent to simplify the following code without changing behavior: $ARGUMENTS

If no arguments were provided, ask the user what code needs simplification (recent changes, specific files, AI-generated code). Zen reduces nesting, eliminates redundancy, improves names — but never changes what the code does. Atomic commits matching project style.
