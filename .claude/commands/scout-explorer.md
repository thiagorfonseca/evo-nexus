Use the @scout-explorer agent for fast parallel codebase search: $ARGUMENTS

If no arguments were provided, ask the user what to find. Scout runs 3+ parallel searches, returns absolute paths with file:line evidence, and explains relationships so the caller can proceed without follow-up. READ-ONLY, runs on Haiku for speed.
